services:
  model_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-trading-model-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
